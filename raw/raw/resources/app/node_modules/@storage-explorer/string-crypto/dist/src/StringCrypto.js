"use strict";
/*!---------------------------------------------------------
 * Copyright (C) Microsoft Corporation. All rights reserved.
 *----------------------------------------------------------*/
Object.defineProperty(exports, "__esModule", { value: true });
exports.StringCrypto = exports.generateRandomBase64String = void 0;
const crypto = require("crypto");
/**
 * @param length Number of characters in the generated string.
 */
function generateRandomBase64String(length) {
    return crypto.randomBytes(Math.ceil(3 / 4 * length)).toString("base64");
}
exports.generateRandomBase64String = generateRandomBase64String;
/**
 * The default padding NodeJS uses: https://nodejs.org/api/crypto.html#ciphersetautopaddingautopadding
 */
const defaultPaddingMode = "pkcs";
const defaultAlgorithm = "aes-256-cbc";
const defaultKeySize = 32;
/**
 * Class that can encrypt/decrypt data.
 */
class StringCrypto {
    _encryptionKey;
    /**
     * @param encryptionKey A string used as the encryption key. The string must have enough bytes.
     * For example, to be used as a key with keySize 32, the encryption key must be at least 32 bytes long.
     */
    constructor(encryptionKey) {
        this._encryptionKey = encryptionKey;
    }
    /**
     * @param plainText Plain text in utf8.
     */
    encrypt(plainText, parameters) {
        const parametersToUse = parameters;
        if (parametersToUse.algorithm !== defaultAlgorithm || parametersToUse.keySize !== defaultKeySize || parametersToUse.paddingMode !== defaultPaddingMode) {
            throw Error("Unsupported encryption parameters.");
        }
        const key = this._encryptionKey.slice(0, parametersToUse.keySize);
        const keyBuffer = Buffer.alloc(key.length, key);
        const ivToUse = parametersToUse.initializationVector ?? generateRandomBase64String(16);
        const ivBuffer = Buffer.alloc(ivToUse.length, ivToUse);
        const cipher = crypto.createCipheriv(parametersToUse.algorithm, keyBuffer, ivBuffer); // CodeQL [js/encryption-with-vulnerable-cipher-mode-node] [js/weak-symmetric-algorithm-node] The algorithm is made a parameter to allow users to use their own algorithms. We ensure that the algorithm we use is allowed.
        const cipherText = cipher.update(plainText, "utf8", "base64") + cipher.final("base64");
        return {
            cipherText: cipherText,
            algorithm: parametersToUse.algorithm,
            initializationVector: ivToUse,
            paddingMode: parametersToUse.paddingMode,
            keySize: parametersToUse.keySize
        };
    }
    /**
     * @param cipherText Encrypted text in base64 encoded format.
     * @returns Plain text in utf8.
     */
    decrypt(encryptionData) {
        const key = this._encryptionKey;
        const keyBuffer = Buffer.alloc(key.length, key);
        const initializationVector = encryptionData.initializationVector;
        const ivBuffer = Buffer.alloc(initializationVector.length, initializationVector);
        const cipherText = encryptionData.cipherText;
        const algorithm = encryptionData.algorithm;
        const decipher = crypto.createDecipheriv(algorithm, keyBuffer, ivBuffer);
        const plainText = decipher.update(cipherText, "base64", "utf8") + decipher.final("utf8");
        return plainText;
    }
    getDefaultParameters() {
        return {
            algorithm: defaultAlgorithm,
            keySize: defaultKeySize,
            paddingMode: defaultPaddingMode
        };
    }
}
exports.StringCrypto = StringCrypto;
//# sourceMappingURL=StringCrypto.js.map